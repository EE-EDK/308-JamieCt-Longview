# 308-Jamie-Ct-3b d-house — Configuration Template
# Copy to config.yaml and fill in values.

project:
  name: "308-Jamie-Ct-3b d-house"        # Project display name
  version: "0.1.0"            # Semantic version

# api_keys:
#   service_name: ${SERVICE_API_KEY}  # Set via environment variable

# collection:
#   rate_limit_per_minute: 5   # API rate limit
#   cache_ttl_hours: 24        # Cache duration
